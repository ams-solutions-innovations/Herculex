# Moved

This tree was retired. The active Wear OS source is `android/wear`, built and
installed alongside the phone app via the root Gradle build — see
`LESSONS.md` for why this tree existed and what went wrong while it did.
