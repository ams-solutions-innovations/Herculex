---
phase: 09-analytics-consolidation-and-soft-delete-correctness
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - test/analytics_soft_delete_test.dart
autonomous: true
requirements: [ANLY-03]
user_setup: []

must_haves:
  truths:
    - "Soft-deleting a completed set (setting its deletedAt) removes it from TrainingSnapshot, weekly tonnage, and top-1RM results"
    - "The regression test fails if soft-delete filtering is ever removed from training_snapshot.dart or analytics_repository.dart"
  artifacts:
    - path: "test/analytics_soft_delete_test.dart"
      provides: "Automated regression coverage for ANLY-03 (the CONTEXT.md D-04 acceptance gate)"
      contains: "deletedAt"
  key_links:
    - from: "test/analytics_soft_delete_test.dart"
      to: "lib/features/analytics/domain/training_snapshot.dart"
      via: "TrainingSnapshot.load(db)"
      pattern: "TrainingSnapshot\\.load"
    - from: "test/analytics_soft_delete_test.dart"
      to: "lib/features/analytics/data/analytics_repository.dart"
      via: "AnalyticsRepository(db).weeklyTonnage() / topOneRms()"
      pattern: "AnalyticsRepository"
---

<objective>
Write the automated regression test that is the acceptance gate for ANLY-03, per CONTEXT.md decision D-04: prove a soft-deleted set is excluded from analytics, not just assert it by inspection.

Purpose: Plans 09-01 and 09-02 added the `deletedAt.isNull()` filtering this test locks in place. Without this test, a future edit could silently drop the filter and regress the cross-device-sync-delete bug this phase exists to fix.
Output: `test/analytics_soft_delete_test.dart`, passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-CONTEXT.md
@.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-01-SUMMARY.md
@.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-02-SUMMARY.md
@test/support/test_database.dart
@test/repository_delete_paths_test.dart
</context>

<interfaces>
<!-- Insert-tree pattern, from test/repository_delete_paths_test.dart lines 34-83 -->
```dart
Future<int> anyExerciseId(AppDatabase db) async {
  final row = await (db.select(db.exerciseCatalog)..limit(1)).getSingle();
  return row.id;
}
// WorkoutSessionsCompanion.insert(startedAt: DateTime(2026))
// WorkoutExercisesCompanion.insert(sessionId: sessionId, exerciseId: exerciseId, orderIndex: 0)
// SetEntriesCompanion.insert(workoutExerciseId: weId, setIndex: 0, weightKg: 60, reps: 5)
```

<!-- Soft-delete write pattern, from lib/features/nutrition/data/nutrition_repository.dart:211-215 -->
```dart
await (_db.update(_db.foods)..where((t) => t.id.equals(id))).write(
  FoodsCompanion(deletedAt: Value(_clock.now())),
);
```

<!-- SetEntries column defaults, from lib/data/local/tables.dart:201-232 -->
`isCompleted` defaults to `false` — the test insert MUST pass `isCompleted: const Value(true)` explicitly, or the set will already be excluded by TrainingSnapshot's own `!set.isCompleted` check regardless of soft-delete, making the test meaningless.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the soft-delete analytics regression test</name>
  <read_first>
    test/repository_delete_paths_test.dart (insert-tree helpers, lines 1-100)
    test/rb05_soft_delete_test.dart (soft-delete test shape/style to match)
    test/support/test_database.dart
    lib/features/analytics/domain/training_snapshot.dart (post-09-01)
    lib/features/analytics/data/analytics_repository.dart (post-09-01)
    lib/data/local/tables.dart (SetEntries, WorkoutSessions, WorkoutExercises column shapes)
  </read_first>
  <files>test/analytics_soft_delete_test.dart</files>
  <behavior>
    - Test 1 ("TrainingSnapshot excludes a soft-deleted set"): insert a session → exercise → completed set (`isCompleted: const Value(true)`, `weightKg: 60`, `reps: 5`) via `openTestDatabase()`. Call `TrainingSnapshot.load(db)` — assert `snapshot.sets` has length 1 and its `set.id` matches the inserted set. Soft-delete the set (`(db.update(db.setEntries)..where((t) => t.id.equals(setId))).write(SetEntriesCompanion(deletedAt: Value(DateTime.now())))`). Call `TrainingSnapshot.load(db)` again — assert `snapshot.sets` is empty.
    - Test 2 ("weeklyTonnage excludes a soft-deleted set"): same insert tree, with `startedAt: DateTime.now()` on the session so it falls in the current week bucket. Call `AnalyticsRepository(db).weeklyTonnage()` — assert the current week's `tonnageKg` is `300` (60 × 5). Soft-delete the set as above. Call `weeklyTonnage()` again — assert the current week's `tonnageKg` is `0`.
    - Test 3 ("topOneRms excludes a soft-deleted set"): same insert tree. Call `AnalyticsRepository(db).topOneRms()` — assert it contains an entry for the inserted exercise. Soft-delete the set. Call `topOneRms()` again — assert the list no longer contains that exercise (empty, since it's the only set).
  </behavior>
  <action>
Create `test/analytics_soft_delete_test.dart` mirroring the structure of `test/rb05_soft_delete_test.dart` (top-level `main()`, `TestWidgetsFlutterBinding.ensureInitialized()`, a local `Future<int> anyExerciseId(AppDatabase db)` helper copied from `repository_delete_paths_test.dart`, one `group('analytics excludes soft-deleted sets', ...)` containing the three tests described in `<behavior>`). Each test opens its own `db = await openTestDatabase()` and calls `addTearDown(db.close)`. Import `package:drift/drift.dart hide isNull, isNotNull`, `package:flutter_test/flutter_test.dart`, `package:herculex/data/local/database.dart`, `package:herculex/features/analytics/domain/training_snapshot.dart`, `package:herculex/features/analytics/data/analytics_repository.dart`, and `support/test_database.dart`.
  </action>
  <verify>
    <automated>flutter test test/analytics_soft_delete_test.dart</automated>
  </verify>
  <done>All three tests in `test/analytics_soft_delete_test.dart` pass; each proves the soft-deleted set contributes to the pre-delete assertion and is absent from the post-delete assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Test-only change against an in-memory database; no production trust boundary touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-04 | Tampering (regression) | `test/analytics_soft_delete_test.dart` | mitigate | Automated test locks in the deletedAt filtering added in Plan 09-01 so a future edit cannot silently remove it without breaking CI. |
</threat_model>

<verification>
`flutter test test/analytics_soft_delete_test.dart` exits 0 with 3 passing tests.
</verification>

<success_criteria>
The soft-delete correctness gate for ANLY-03 is enforced by an automated test, not a manual check.
</success_criteria>

<output>
Create `.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-03-SUMMARY.md` when done
</output>
