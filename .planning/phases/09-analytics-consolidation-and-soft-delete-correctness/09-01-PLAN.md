---
phase: 09-analytics-consolidation-and-soft-delete-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/analytics/domain/training_snapshot.dart
  - lib/features/analytics/data/analytics_repository.dart
  - lib/features/analytics/domain/balance_analyzer.dart
  - lib/features/analytics/domain/biometric_correlations.dart
autonomous: true
requirements: [ANLY-03, ANLY-04]
user_setup: []

must_haves:
  truths:
    - "A soft-deleted (deletedAt set) completed set no longer contributes to weekly tonnage, top-1RM, or TrainingSnapshot-derived results"
    - "Push/pull balance is computed from effective load (tonnageKg), not a raw set count"
    - "Sleep-vs-RPE and resting-HR-vs-tonnage correlations never substitute fabricated points when the real sample is small — they return the real (possibly empty/low-confidence) result"
  artifacts:
    - path: "lib/features/analytics/domain/training_snapshot.dart"
      provides: "TrainingSnapshot.load filters deletedAt on every SyncTombstone table it reads"
      contains: "deletedAt.isNull()"
    - path: "lib/features/analytics/data/analytics_repository.dart"
      provides: "weeklyTonnage and topOneRms exclude soft-deleted sets/exercises/sessions"
      contains: "deletedAt.isNull()"
    - path: "lib/features/analytics/domain/balance_analyzer.dart"
      provides: "BalanceAnalyzer.summary(List<ResolvedSet>) computing tonnage-weighted push/pull split"
      contains: "List<ResolvedSet>"
    - path: "lib/features/analytics/domain/biometric_correlations.dart"
      provides: "BiometricCorrelations.sleepVsRpe/restingHrVsTonnage accepting List<ResolvedSet>, no mock fallback"
      contains: "List<ResolvedSet>"
  key_links:
    - from: "lib/features/analytics/domain/training_snapshot.dart"
      to: "lib/data/local/tables.dart"
      via: "deletedAt column from the SyncTombstone mixin"
      pattern: "deletedAt\\.isNull\\(\\)"
    - from: "lib/features/analytics/domain/balance_analyzer.dart"
      to: "lib/features/analytics/domain/training_snapshot.dart"
      via: "ResolvedSet.tonnageKg / ResolvedSet.effectiveKg"
      pattern: "tonnageKg|effectiveKg"
---

<objective>
Make every analytics read path in this plan's scope exclude soft-deleted rows and compute from effective load instead of raw weight.

Purpose: This is the correctness foundation the rest of Phase 9 builds on — Plan 09-02 rewires the providers to consume these fixed functions, so the fix has to land here first.
Output: `training_snapshot.dart` and `analytics_repository.dart` never surface soft-deleted rows; `balance_analyzer.dart` and `biometric_correlations.dart` accept `List<ResolvedSet>` and use effective load; both correlation fallback-mock blocks are gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-CONTEXT.md
@lib/data/local/tables.dart
</context>

<interfaces>
<!-- ResolvedSet / TrainingSnapshot, from lib/features/analytics/domain/training_snapshot.dart -->
```dart
class ResolvedSet {
  final SetEntryData set;
  final WorkoutExerciseData workoutExercise;
  final WorkoutSessionData session;
  final ExerciseCatalogData exercise;
  final SetType setType;
  final List<BandContribution> bands;
  final List<String> accessoryNames;
  final double forearmMultiplier;
  final double effectiveKg;
  double get tonnageKg; // effectiveKg-based, setType-aware
  String get equipmentVariant;
  String get accessoryCombo;
  int get cnsScore;
}

class TrainingSnapshot {
  final List<ResolvedSet> sets;
  final List<ExerciseMuscleData> exerciseMuscles;
  static Future<TrainingSnapshot> load(AppDatabase db);
}
```

<!-- Existing soft-delete filter pattern, from lib/features/nutrition/data/nutrition_repository.dart -->
```dart
..where((t) => t.deletedAt.isNull())
```

<!-- Tables carrying SyncTombstone (deletedAt), from lib/data/local/tables.dart -->
ExerciseCatalog, WorkoutSessions, WorkoutExercises, SetEntries, SetAccessories, SetBands, Accessories, Bands all `extends Table with SyncColumns, SyncTombstone`. ExerciseMuscles does NOT carry SyncTombstone (static seed data — do not add a filter for it).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Filter soft-deleted rows out of TrainingSnapshot and AnalyticsRepository</name>
  <read_first>
    lib/features/analytics/domain/training_snapshot.dart
    lib/features/analytics/data/analytics_repository.dart
    lib/features/nutrition/data/nutrition_repository.dart (lines 15-30, 80-90 — the `deletedAt.isNull()` pattern to replicate)
    lib/data/local/tables.dart (lines 1-30 for the SyncTombstone mixin, plus the 8 table classes it applies to)
  </read_first>
  <files>lib/features/analytics/domain/training_snapshot.dart, lib/features/analytics/data/analytics_repository.dart</files>
  <action>
In `TrainingSnapshot.load` (training_snapshot.dart), add `..where((t) => t.deletedAt.isNull())` to every one of the 8 `db.select(db.X).get()` calls whose table carries `SyncTombstone`: `setEntries`, `workoutExercises`, `workoutSessions`, `exerciseCatalog`, `setAccessories`, `setBands`, `accessories`, `bands`. Leave `db.select(db.exerciseMuscles).get()` unfiltered — that table has no `deletedAt` column. Keep the existing `Future.wait` structure; each `db.select(...)` call becomes `(db.select(db.x)..where((t) => t.deletedAt.isNull())).get()`.

In `analytics_repository.dart`, add the same `deletedAt.isNull()` filter to `weeklyTonnage()`'s join query (on `setEntries`, `workoutExercises`, `workoutSessions` — AND them into the existing `..where(...)` clause alongside `isCompleted`/`isWarmup`/`startedAt`) and to `topOneRms()`'s join query (on `setEntries`, `workoutExercises`, `exerciseCatalog`). Use `&` to combine with the existing predicates, matching the file's existing `&`-chained where-clause style.
  </action>
  <verify>
    <automated>flutter analyze lib/features/analytics/domain/training_snapshot.dart lib/features/analytics/data/analytics_repository.dart</automated>
  </verify>
  <done>Both files compile clean; every SyncTombstone-carrying table read in these two files has a `deletedAt.isNull()` predicate; `exerciseMuscles` is unfiltered.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite BalanceAnalyzer to consume ResolvedSet and use effective load</name>
  <read_first>
    lib/features/analytics/domain/balance_analyzer.dart
    lib/features/analytics/domain/training_snapshot.dart (for ResolvedSet's `tonnageKg`, `exercise`, `workoutExercise`, `set.isWarmup`/`isCompleted` — already excluded upstream, no need to re-check in this function)
  </read_first>
  <files>lib/features/analytics/domain/balance_analyzer.dart</files>
  <action>
Change `BalanceAnalyzer.summary`'s signature from `({required List<SetEntryData> sets, required List<WorkoutExerciseData> workoutExercises, required List<ExerciseCatalogData> catalog})` to `({required List<ResolvedSet> sets})`. Remove the `exerciseMap`/`weMap` lookups (no longer needed — `ResolvedSet.exercise` is already resolved) and the `if (!set.isCompleted) continue` guard (TrainingSnapshot already excludes incomplete/warmup sets). Replace `pushSets++` / `pullSets++` (currently a raw set count) with `pushTonnage += resolvedSet.tonnageKg` / `pullTonnage += resolvedSet.tonnageKg` using the same `force`/`primaryMuscle` classification logic already present (read from `resolvedSet.exercise.force` / `resolvedSet.exercise.primaryMuscle`). Compute `pushPct`/`pullPct` from `pushTonnage`/`pullTonnage` totals (same 100.0 * share formula, same >25.0 asymmetry threshold) instead of set counts. Import `training_snapshot.dart` for `ResolvedSet`; the `../../../data/local/database.dart` import can be dropped if nothing else in the file needs it after the rewrite (check with `flutter analyze` for unused-import).
  </action>
  <verify>
    <automated>flutter analyze lib/features/analytics/domain/balance_analyzer.dart</automated>
  </verify>
  <done>`BalanceAnalyzer.summary` takes `List<ResolvedSet>`, sums `tonnageKg` per push/pull category instead of counting sets, and the file has no unused imports.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite BiometricCorrelations to consume ResolvedSet, use tonnageKg, and drop the mock fallback</name>
  <read_first>
    lib/features/analytics/domain/biometric_correlations.dart
    lib/features/analytics/domain/training_snapshot.dart (for `ResolvedSet.session`, `.tonnageKg`, `.set.rpeX10`)
  </read_first>
  <files>lib/features/analytics/domain/biometric_correlations.dart</files>
  <action>
Change both `sleepVsRpe` and `restingHrVsTonnage` signatures from `({required List<HealthSampleData> healthSamples, required List<SetEntryData> sets, required List<WorkoutExerciseData> workoutExercises, required List<WorkoutSessionData> sessions})` to `({required List<HealthSampleData> healthSamples, required List<ResolvedSet> resolvedSets})`. Derive the distinct session list from `resolvedSets.map((r) => r.session)` deduplicated by `session.id` (a `Map<int, WorkoutSessionData>` keyed by id, then `.values`) instead of taking a separate `sessions` parameter. For each session, filter `resolvedSets.where((r) => r.session.id == session.id)` instead of joining through `workoutExercises`/`sets` by id sets.

In `sleepVsRpe`: keep reading `s.set.rpeX10` per matched `ResolvedSet` for the average-RPE calculation (unchanged math, just sourced from `ResolvedSet.set.rpeX10` instead of the old `SetEntryData.rpeX10`).

In `restingHrVsTonnage`: replace `sessionTonnage += s.weightKg * s.reps` with `sessionTonnage += r.tonnageKg` (the effective-load-aware tonnage, `r` being the matched `ResolvedSet`).

**Delete the `if (points.length < 3) { return const BiometricCorrelationResult(points: [...5 hardcoded points...], r2: ..., sampleSize: 5) }` mock-fallback block from both functions entirely.** Let the function fall through to the real `_calculateR2(points)` computation and return the actual (possibly small or empty) `points`/`sampleSize` — the existing `BiometricCorrelationResult.interpretation` getter already has the correct `"Insufficient sessions recorded yet."` copy for `sampleSize < 3`; it was previously unreachable because the mock block always forced `sampleSize: 5`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/analytics/domain/biometric_correlations.dart</automated>
  </verify>
  <done>Both functions take `List<ResolvedSet>` instead of separate raw table lists, `restingHrVsTonnage` sums `tonnageKg`, and neither function contains a hardcoded fallback points list — low-sample runs return the real `sampleSize` and let `interpretation` handle the "Insufficient sessions" case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local DB → analytics domain layer | All data is device-local, already-authenticated user data; no untrusted external input crosses this boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-01 | Tampering / Integrity | `training_snapshot.dart`, `analytics_repository.dart` | mitigate | Add `deletedAt.isNull()` to every SyncTombstone-table read so a cross-device soft-delete cannot silently reappear in derived analytics (Task 1). |
| T-09-02 | Information Disclosure (of fabricated data as real) | `biometric_correlations.dart` | mitigate | Remove the hardcoded mock-fallback point sets so the UI never presents synthetic correlation data as if it were the user's real history (Task 3). |
</threat_model>

<verification>
- `flutter analyze` reports no new errors/warnings in the 4 modified files.
- Manual read-through confirms `deletedAt.isNull()` appears on all 8 tombstoned-table reads in `training_snapshot.dart`, and on the relevant joins in `analytics_repository.dart`.
- Manual read-through confirms no `CorrelationPoint(5.5, 8.5)`-style hardcoded fallback list remains in `biometric_correlations.dart`.
</verification>

<success_criteria>
`training_snapshot.dart` and `analytics_repository.dart` never return soft-deleted rows; `balance_analyzer.dart` and `biometric_correlations.dart` compile against `List<ResolvedSet>`, use `tonnageKg` (effective load) instead of raw weight, and contain no fabricated fallback data.
</success_criteria>

<output>
Create `.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-01-SUMMARY.md` when done
</output>
