---
phase: 09-analytics-consolidation-and-soft-delete-correctness
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/features/analytics/presentation/analytics_providers.dart
  - lib/features/analytics/presentation/insights_view.dart
  - lib/features/analytics/domain/muscle_recovery.dart
  - lib/features/analytics/domain/cns_fatigue.dart
  - lib/features/analytics/presentation/recovery_heatmap_widget.dart
autonomous: true
requirements: [ANLY-01, ANLY-02]
user_setup: []

must_haves:
  truths:
    - "Insights shows exactly one recovery card (RecoveryDetailCard); the legacy coarse recovery card is gone"
    - "pushPullBalanceProvider, sleepVsRpeProvider, and hrVsTonnageProvider all read from trainingSnapshotProvider instead of independent unfiltered table scans"
    - "No dead analytics provider or domain code remains (cnsFatigueProvider, muscleRecoveryProvider, muscle_recovery.dart, cns_fatigue.dart, recovery_heatmap_widget.dart)"
  artifacts:
    - path: "lib/features/analytics/presentation/analytics_providers.dart"
      provides: "pushPullBalanceProvider/sleepVsRpeProvider/hrVsTonnageProvider sourced from trainingSnapshotProvider; cnsFatigueProvider and muscleRecoveryProvider removed"
      contains: "trainingSnapshotProvider"
    - path: "lib/features/analytics/presentation/insights_view.dart"
      provides: "Insights screen with a single recovery card"
      not_contains: "_RecoveryCard"
  key_links:
    - from: "lib/features/analytics/presentation/analytics_providers.dart"
      to: "lib/features/analytics/domain/balance_analyzer.dart"
      via: "BalanceAnalyzer.summary(sets: snapshot.sets)"
      pattern: "BalanceAnalyzer\\.summary"
    - from: "lib/features/analytics/presentation/insights_view.dart"
      to: "lib/features/analytics/presentation/cns_recovery_cards.dart"
      via: "RecoveryDetailCard as the sole recovery widget"
      pattern: "RecoveryDetailCard"
---

<objective>
Retarget the remaining independent analytics providers onto the shared `trainingSnapshotProvider`, delete the now-fully-dead legacy recovery engine and its UI card, and remove the dead `cnsFatigueProvider`.

Purpose: Plan 09-01 fixed the domain functions this plan wires up; this plan finishes the consolidation so Insights has exactly one data path per metric and exactly one recovery card, per CONTEXT.md D-01/D-02.
Output: `analytics_providers.dart` has one recovery/CNS/balance/correlation data path; `insights_view.dart` shows only `RecoveryDetailCard`; `muscle_recovery.dart`, `cns_fatigue.dart`, and `recovery_heatmap_widget.dart` are deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-CONTEXT.md
@.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-01-SUMMARY.md
</context>

<interfaces>
<!-- New signatures from Plan 09-01, lib/features/analytics/domain/balance_analyzer.dart and biometric_correlations.dart -->
```dart
class BalanceAnalyzer {
  static BalanceResult summary({required List<ResolvedSet> sets});
}

class BiometricCorrelations {
  static BiometricCorrelationResult sleepVsRpe({
    required List<HealthSampleData> healthSamples,
    required List<ResolvedSet> resolvedSets,
  });
  static BiometricCorrelationResult restingHrVsTonnage({
    required List<HealthSampleData> healthSamples,
    required List<ResolvedSet> resolvedSets,
  });
}
```

<!-- trainingSnapshotProvider, from lib/features/analytics/presentation/analytics_providers.dart -->
```dart
final trainingSnapshotProvider = FutureProvider<TrainingSnapshot>((ref) async {
  ref.watch(recentSessionsProvider);
  return TrainingSnapshot.load(ref.watch(appDatabaseProvider));
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retarget balance/correlation providers onto trainingSnapshotProvider; delete dead providers</name>
  <read_first>
    lib/features/analytics/presentation/analytics_providers.dart
    lib/features/analytics/domain/balance_analyzer.dart (post-09-01, for the new `summary(sets: List<ResolvedSet>)` signature)
    lib/features/analytics/domain/biometric_correlations.dart (post-09-01, for the new `resolvedSets` parameter name)
  </read_first>
  <files>lib/features/analytics/presentation/analytics_providers.dart</files>
  <action>
Delete `cnsFatigueProvider` (lines 60-74, the `FutureProvider<CnsFatigueResult>` that queries `setEntries`/`workoutExercises`/`exerciseCatalog`/`externalWorkoutsProvider` and calls `CnsFatigue.compute`) — it is never watched by any widget.

Delete `muscleRecoveryProvider` (the `FutureProvider<List<MuscleRecoveryResult>>` that queries `setEntries`/`workoutExercises`/`exerciseCatalog`/`exerciseMuscles` and calls `MuscleRecovery.compute`) — its only consumer, `_RecoveryCard`, is removed in Task 2.

Remove the now-unused `import '../domain/cns_fatigue.dart';` and `import '../domain/muscle_recovery.dart';` lines.

Rewrite `pushPullBalanceProvider` to: `final snapshot = await ref.watch(trainingSnapshotProvider.future); return BalanceAnalyzer.summary(sets: snapshot.sets);` — remove the three independent `db.select(...).get()` calls it previously made.

Rewrite `sleepVsRpeProvider` to keep its own `db.select(db.healthSamples).get()` call (health samples aren't part of `TrainingSnapshot`) but replace the `sets`/`workoutExercises`/`sessions` queries with `final snapshot = await ref.watch(trainingSnapshotProvider.future);` and call `BiometricCorrelations.sleepVsRpe(healthSamples: healthSamples, resolvedSets: snapshot.sets)`.

Rewrite `hrVsTonnageProvider` the same way: keep the `healthSamples` query, replace `sets`/`workoutExercises`/`sessions` with `trainingSnapshotProvider`, call `BiometricCorrelations.restingHrVsTonnage(healthSamples: healthSamples, resolvedSets: snapshot.sets)`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/analytics/presentation/analytics_providers.dart</automated>
  </verify>
  <done>`cnsFatigueProvider` and `muscleRecoveryProvider` no longer exist in the file; `pushPullBalanceProvider`, `sleepVsRpeProvider`, and `hrVsTonnageProvider` all obtain sets via `ref.watch(trainingSnapshotProvider.future)`; no unused imports remain; file compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the duplicate recovery card and delete now-dead analytics files</name>
  <read_first>
    lib/features/analytics/presentation/insights_view.dart
    lib/features/analytics/presentation/recovery_heatmap_widget.dart
    lib/features/analytics/domain/muscle_recovery.dart
    lib/features/analytics/domain/cns_fatigue.dart
  </read_first>
  <files>lib/features/analytics/presentation/insights_view.dart, lib/features/analytics/domain/muscle_recovery.dart, lib/features/analytics/domain/cns_fatigue.dart, lib/features/analytics/presentation/recovery_heatmap_widget.dart</files>
  <action>
In `insights_view.dart`: remove the `const _RecoveryCard(),` widget instantiation and its preceding `const SizedBox(height: 24),` from the `InsightsView` build method's children list (currently the first two entries after the header, immediately before `const RecoveryDetailCard()`). Delete the entire `_RecoveryCard` class definition (the `ConsumerWidget` that watches `muscleRecoveryProvider` and renders `RecoveryHeatmapWidget`, including its private `_legendItem` helper). Remove the now-unused `import 'recovery_heatmap_widget.dart';` line.

Delete these three files entirely (now fully dead — confirmed no remaining references after the above edit and Task 1): `lib/features/analytics/domain/muscle_recovery.dart`, `lib/features/analytics/domain/cns_fatigue.dart`, `lib/features/analytics/presentation/recovery_heatmap_widget.dart`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/analytics/</automated>
  </verify>
  <done>`insights_view.dart` contains no `_RecoveryCard` class or reference; `RecoveryDetailCard` is the first card rendered; the three dead files no longer exist on disk; `flutter analyze lib/features/analytics/` reports no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only removes dead code and reroutes existing providers to an already-vetted shared data source (`trainingSnapshotProvider`, soft-delete-filtered as of Plan 09-01); no new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-03 | Tampering (stale duplicate UI) | `insights_view.dart` | mitigate | Remove `_RecoveryCard` so the app cannot show two disagreeing recovery numbers for the same muscle group (Task 2). |
</threat_model>

<verification>
- `flutter analyze lib/features/analytics/` reports no errors after both tasks.
- `grep -rn "muscleRecoveryProvider\|cnsFatigueProvider\|_RecoveryCard\|RecoveryHeatmapWidget" lib/` returns no matches.
</verification>

<success_criteria>
`analytics_providers.dart` has one shared, soft-delete-filtered, effective-load-aware data path for recovery/CNS/balance/correlation; `insights_view.dart` renders exactly one recovery card; the three superseded files are deleted.
</success_criteria>

<output>
Create `.planning/phases/09-analytics-consolidation-and-soft-delete-correctness/09-02-SUMMARY.md` when done
</output>
