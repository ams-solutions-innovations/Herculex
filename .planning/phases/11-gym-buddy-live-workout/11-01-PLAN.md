---
phase: 11-gym-buddy-live-workout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/supabase-migrations.md
  - supabase/config.toml
  - .gitignore
autonomous: false
requirements: [BUD-04, BUD-05]

must_haves:
  truths:
    - "A developer on a clean checkout can apply a new Supabase migration with a documented command sequence instead of pasting SQL into Studio"
    - "The repo is linked to project ref ldzgyzigvbwofbswitrv, not to jioesomepkauponjrena (SummitSki)"
    - "`supabase migration list` reports 0001-0010 as already applied remotely, so a push of 0011 will not attempt to re-run them"
  artifacts:
    - path: "docs/supabase-migrations.md"
      provides: "The migration workflow: install, login, link, list, push, and the remote-history reconciliation for 0001-0010"
      min_lines: 40
    - path: "supabase/config.toml"
      provides: "project_id linking the repo to ldzgyzigvbwofbswitrv"
      contains: "project_id"
  key_links:
    - from: "supabase/config.toml"
      to: "Supabase project ldzgyzigvbwofbswitrv"
      via: "project_id set by `supabase link --project-ref`"
      pattern: "project_id"
    - from: "docs/supabase-migrations.md"
      to: "supabase/migrations/*.sql"
      via: "documented `supabase db push` workflow"
      pattern: "db push"
---

<objective>
Install and wire up the Supabase CLI so `supabase/migrations/0011_buddy_sessions.sql` can be applied
by command rather than by a human paste into the SQL Editor, and record the workflow in the repo so
it stops being tribal knowledge.

Purpose: Locked decision 8. Every server-side requirement of this phase — BUD-04's durable log,
BUD-05's policies, BUD-01's join-token RPCs — is unverifiable until migrations can be applied
repeatably. `docs/rb02-sync-verification.md` records that `0001`-`0010` were applied "via the Supabase
MCP tools" against the *wrong project* (`jioesomepkauponjrena` = SummitSki) and re-applied to
`ldzgyzigvbwofbswitrv` on 2026-08-15, with no reproducible command written down anywhere.

Output: A linked `supabase/config.toml`, a `docs/supabase-migrations.md` workflow document, and a
verified remote migration history.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gym-buddy-live-workout/11-RESEARCH.md
@.planning/phases/11-gym-buddy-live-workout/11-CONTEXT.md
@docs/rb02-sync-verification.md
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the Supabase CLI and record its version</name>
  <read_first>
    - supabase/config.toml (currently holds only the `[functions.gemini-analyze]` block — no `project_id`)
    - docs/rb02-sync-verification.md (lines 1-40: the wrong-project correction notice and the "applied via Supabase MCP tools" claim)
    - .planning/phases/11-gym-buddy-live-workout/11-RESEARCH.md § Environment Availability (Supabase CLI marked ✗)
  </read_first>
  <action>
    Install the Supabase CLI on this Windows machine. `winget` and `npx` are both on PATH; `scoop`,
    `psql` and a pre-existing `supabase` binary are not. Preferred order:

    1. `winget install --id Supabase.CLI --accept-source-agreements --accept-package-agreements`
    2. If winget has no such package id, fall back to `npm install -g supabase` — note that the
       Supabase CLI explicitly refuses global npm installs in recent versions; if it does, use the
       scoop bucket route (`scoop bucket add supabase https://github.com/supabase/scoop-bucket.git`
       then `scoop install supabase`) after installing scoop, or download the Windows release archive
       from `github.com/supabase/cli/releases` into a directory already on PATH.

    Record the exact route that worked and the resolved version in the summary. Do NOT invoke the CLI
    as `npx --yes supabase@latest` anywhere — pin a real installed binary.

    Then confirm the subcommands this phase depends on exist in the installed version:
    `supabase link --help`, `supabase db push --help`, `supabase migration list --help`. Note in the
    summary whether `db push` exposes a non-interactive confirmation flag (`--yes` / `-y`) or whether
    it always prompts, because plan 11-05 sequences a blocking human checkpoint around that prompt.
  </action>
  <verify>
    <automated>supabase --version</automated>
  </verify>
  <acceptance_criteria>
    - `supabase --version` prints a version string and exits 0
    - `supabase db push --help` exits 0 and its output is captured in the plan summary
    - `supabase migration list --help` exits 0
    - The summary states the install route used and whether `db push` can be run non-interactively
    - No file in the repo invokes the CLI via `npx --yes supabase@latest`
  </acceptance_criteria>
  <done>The `supabase` binary is on PATH and its version and `db push` interactivity are recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Authenticate and link the repo to project ldzgyzigvbwofbswitrv</name>
  <read_first>
    - supabase/config.toml
    - .gitignore (check whether `supabase/.temp/` and `.env` are already ignored)
    - ~/.claude memory note: Herculex = `ldzgyzigvbwofbswitrv`; `jioesomepkauponjrena` is SummitSki
  </read_first>
  <what-built>
    The CLI is installed and its subcommands verified (Task 1). What remains cannot be automated:
    obtaining a `SUPABASE_ACCESS_TOKEN` and the project's database password.
  </what-built>
  <how-to-verify>
    1. Create a personal access token at `https://supabase.com/dashboard/account/tokens`.
    2. In the shell that will run the next steps, export it:
       `export SUPABASE_ACCESS_TOKEN=sbp_...`
       (Do NOT write it into any repo file. If a `.env` is used, confirm `.env` is in `.gitignore`
       and add it if not.)
    3. From the repo root run:
       `supabase link --project-ref ldzgyzigvbwofbswitrv`
       Confirm the ref is `ldzgyzigvbwofbswitrv`. If the CLI prints `jioesomepkauponjrena`, stop —
       that is the SummitSki project and linking to it is the exact defect
       `docs/rb02-sync-verification.md` was corrected for.
    4. Run `supabase migration list` and paste the output back. Expected: `0001` through `0010` shown
       as present both locally and remotely.
    5. If the remote column is empty for migrations that are in fact applied (they were applied
       outside the CLI, so `supabase_migrations.schema_migrations` may not have their rows), run
       `supabase migration repair --status applied 0001 0002 0003 0004 0005 0006 0007 0008 0009 0010`
       and re-run `supabase migration list`. Report which of the two situations occurred.
  </how-to-verify>
  <resume-signal>Paste the `supabase migration list` output and type "linked" — or describe the failure.</resume-signal>
  <acceptance_criteria>
    - `supabase/config.toml` contains a `project_id` line whose value is `ldzgyzigvbwofbswitrv`
    - `supabase migration list` shows `0001`-`0010` with a non-empty Remote column
    - `git status --porcelain` shows no untracked or modified file containing the string `sbp_`
    - `.gitignore` contains `supabase/.temp` (add it if absent)
  </acceptance_criteria>
  <done>The repo is linked to the Herculex project and remote migration history matches `supabase/migrations/`.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/supabase-migrations.md</name>
  <read_first>
    - docs/rb02-sync-verification.md (the document this one supersedes for the "how do migrations reach the server" question — link to it, do not duplicate its findings)
    - docs/supabase-auth-wear-setup.md (the repo's existing convention for a Supabase-operations doc: what tone, how commands are presented)
    - supabase/config.toml (post-link, to quote the real `project_id`)
  </read_first>
  <action>
    Create `docs/supabase-migrations.md`. It must contain, as literal copy-pasteable commands:

    - Install: the exact route that worked in Task 1, with the version installed.
    - Auth: `export SUPABASE_ACCESS_TOKEN=...` and where to mint it, with an explicit "never commit
      this" note and the `.gitignore` entries that back it up.
    - Link: `supabase link --project-ref ldzgyzigvbwofbswitrv`, with a bolded warning that
      `jioesomepkauponjrena` is the SummitSki project and was wrongly configured repo-wide until
      2026-08-15.
    - Inspect: `supabase migration list`, and what to do when the Remote column is empty for a
      migration that was applied outside the CLI (`supabase migration repair --status applied <ver>`).
    - Apply: `supabase db push`, including whether it prompts and how (or whether) that prompt can be
      suppressed, as established in Task 1.
    - Naming: new migrations follow the existing four-digit `NNNN_snake_case_description.sql` scheme
      already used by `0001`-`0010`, not the CLI's default timestamp scheme produced by
      `supabase migration new`. State that a hand-named file dropped into `supabase/migrations/` is
      the repo's convention and that `db push` picks it up by lexical order.
    - A "Do not" section: do not edit an already-applied migration file; do not run `supabase db reset`
      against the linked remote; do not run `supabase db pull` and commit its output, because it would
      rewrite `0003_sync_rls.sql`, which is frozen and hash-pinned by
      `test/buddy/rls_frozen_test.dart` (see BUD-05).

    Add a one-line pointer to this file from the top of `docs/rb02-sync-verification.md` so the stale
    "applied via the Supabase MCP tools" note leads somewhere current.
  </action>
  <verify>
    <automated>test -f docs/supabase-migrations.md && grep -q "ldzgyzigvbwofbswitrv" docs/supabase-migrations.md && grep -q "db push" docs/supabase-migrations.md && grep -q "0003_sync_rls.sql" docs/supabase-migrations.md && grep -q "supabase-migrations.md" docs/rb02-sync-verification.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/supabase-migrations.md` exists and is at least 40 lines
    - It contains the literal strings `ldzgyzigvbwofbswitrv`, `supabase link --project-ref`, `supabase migration list`, `supabase db push`, `supabase migration repair`
    - It contains a "Do not" section naming `0003_sync_rls.sql` as frozen
    - `docs/rb02-sync-verification.md` contains a link to `docs/supabase-migrations.md`
    - `grep -c "jioesomepkauponjrena" docs/supabase-migrations.md` is at least 1 (the warning is present)
  </acceptance_criteria>
  <done>The migration workflow is reproducible from the repo alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → Supabase control plane | A personal access token with full project authority crosses here |
| repo working tree → git history | A leaked token committed to the repo is unrecoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Information disclosure | `SUPABASE_ACCESS_TOKEN` | high | mitigate | Token lives only in the shell environment; Task 2's acceptance criteria greps the working tree for `sbp_`; `.gitignore` covers `supabase/.temp` and `.env` |
| T-11-12 | Elevation of privilege | wrong project link | high | mitigate | `project_id` asserted equal to `ldzgyzigvbwofbswitrv`; the SummitSki ref is named as a stop-condition in Task 2 |
| T-11-05 | Elevation of privilege | `supabase db pull` rewriting `0003_sync_rls.sql` | high | mitigate | The workflow doc forbids committing `db pull` output; `test/buddy/rls_frozen_test.dart` (plan 11-02) hash-pins the file |
| T-11-SC | Tampering | CLI install channel | medium | mitigate | Install from winget / official GitHub releases / the official scoop bucket only; no `npx --yes <pkg>@latest` invocation is permitted anywhere in the repo |
</threat_model>

<verification>
- `supabase --version` exits 0
- `grep project_id supabase/config.toml` prints `ldzgyzigvbwofbswitrv`
- `supabase migration list` shows 0001-0010 applied remotely
- `docs/supabase-migrations.md` exists and passes Task 3's grep gate
</verification>

<success_criteria>
A developer with a personal access token can run three documented commands and apply a pending
migration to the Herculex project, and the previously-undocumented mechanism from
`docs/rb02-sync-verification.md` is superseded by a file in the repo.
</success_criteria>

<output>
Create `.planning/phases/11-gym-buddy-live-workout/11-01-SUMMARY.md` when done.
</output>
