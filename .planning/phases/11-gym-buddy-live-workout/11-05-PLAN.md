---
phase: 11-gym-buddy-live-workout
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-04"]
files_modified:
  - test/sync/live_buddy_test.dart
  - docs/supabase-migrations.md
autonomous: false
requirements: [BUD-01, BUD-04, BUD-05, BUD-06]

must_haves:
  truths:
    - "The four buddy tables, five functions and one trigger exist in the live project ldzgyzigvbwofbswitrv"
    - "A real authenticated client can create a session, receive a join token, and redeem it exactly once"
    - "A second redemption of the same token fails with the same message as an unknown token"
    - "A participant whose left_at is set is refused by buddy_append_event"
    - "realtime.send resolved with the assumed signature — assumption A1 is closed"
  artifacts:
    - path: "test/sync/live_buddy_test.dart"
      provides: "Live server-side smoke suite for the 0011 surface, self-skipping without credentials"
      min_lines: 120
  key_links:
    - from: "test/sync/live_buddy_test.dart"
      to: "public.buddy_create_session / buddy_join_session / buddy_append_event"
      via: "SupabaseClient.rpc against the live project"
      pattern: "buddy_append_event"
    - from: "supabase/migrations/0011_buddy_sessions.sql"
      to: "project ldzgyzigvbwofbswitrv"
      via: "supabase db push"
      pattern: "db push"
---

<objective>
Apply `0011_buddy_sessions.sql` to the live Supabase project and prove the server surface behaves,
before any client code is written against it.

Purpose: `11-CONTEXT.md` § Migrations are automated via the Supabase CLI, and the phase brief's
blocking schema-push requirement. Flutter build and type checks pass whether or not the migration was
applied, so without this step every later plan verifies into a false-positive state. This plan is the
boundary: nothing after it may assume server-side behaviour that has not been observed.

Output: the migration applied, and `test/sync/live_buddy_test.dart` holding a passing smoke suite that
plan 11-12 later extends.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gym-buddy-live-workout/11-RESEARCH.md
@supabase/migrations/0011_buddy_sessions.sql
@docs/supabase-migrations.md
@test/sync/live_round_trip_test.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] supabase db push against ldzgyzigvbwofbswitrv</name>
  <read_first>
    - docs/supabase-migrations.md (written in plan 11-01 — the apply section, including whether `db push` prompts and how it was determined)
    - supabase/migrations/0011_buddy_sessions.sql in full, especially the `do $$` preflight guard on `realtime.send` — its exception message is the expected failure mode if research assumption A1 is wrong
    - supabase/config.toml (confirm `project_id` is `ldzgyzigvbwofbswitrv` before running anything)
  </read_first>
  <what-built>
    Plan 11-01 installed and linked the CLI. Plan 11-04 wrote the complete migration and passed a
    static review against the frozen-policy constraint and every research pitfall. What remains needs
    a `SUPABASE_ACCESS_TOKEN` in the environment and, depending on the installed CLI version, an
    interactive confirmation prompt that cannot be suppressed.
  </what-built>
  <how-to-verify>
    1. Confirm the environment: `supabase projects list` should mark `ldzgyzigvbwofbswitrv` as linked.
       If it errors on auth, re-export `SUPABASE_ACCESS_TOKEN`.
    2. Dry-run the diff first: `supabase migration list`. `0011` must appear as Local-only.
    3. Apply: `supabase db push`. Answer the confirmation prompt if one appears.
    4. Expected success: the CLI reports `0011_buddy_sessions` applied.
       Expected *informative* failure: the preflight `do $$` block raises with the `realtime.send`
       signature message. If that happens, run
       `select pg_get_functiondef('realtime.send'::regproc);` from the Supabase dashboard SQL editor,
       paste the real signature back, and stop — plan 11-04 must be amended, not worked around here.
    5. Re-run `supabase migration list` and confirm `0011` now shows in the Remote column.
    6. Paste both the `db push` output and the final `migration list` output back.
  </how-to-verify>
  <resume-signal>Paste the `supabase db push` and `supabase migration list` output and type "applied" — or paste the failure.</resume-signal>
  <acceptance_criteria>
    - `supabase migration list` shows `0011` present in both the Local and Remote columns
    - The `db push` output contains no error and no `NOTICE` about a skipped statement
    - The plan summary records the literal `realtime.send` signature that satisfied the preflight guard, closing research assumption A1
    - `docs/supabase-migrations.md` gains a short "Applied migrations" line recording that `0011` was pushed on this date by this route
  </acceptance_criteria>
  <done>`0011_buddy_sessions.sql` is applied to the Herculex project and the CLI reports it as remote.</done>
</task>

<task type="auto">
  <name>Task 2: Live smoke suite for the 0011 surface</name>
  <read_first>
    - test/sync/live_round_trip_test.dart lines 25-70 (the `_email`/`_password`/`_email2`/`_password2` dart-define fixture, the "a bare SupabaseClient is pure Dart so this runs under plain flutter test" note, and `_NoRealtimeBackend`) and lines 440-456 (`outsiderSkip()`)
    - supabase/migrations/0011_buddy_sessions.sql (the exact RPC names, argument names and return shapes being called)
    - .planning/phases/11-gym-buddy-live-workout/11-VALIDATION.md (the live command this file must answer to)
  </read_first>
  <action>
    Create `test/sync/live_buddy_test.dart`. Follow `live_round_trip_test.dart`'s structure exactly:
    top-level `String.fromEnvironment` constants for `SUPABASE_URL`, `SUPABASE_ANON_KEY`,
    `SUPABASE_TEST_EMAIL`/`_PASSWORD` and `SUPABASE_TEST_EMAIL_2`/`_PASSWORD_2`; a `skipReason` that
    self-skips the whole group when credentials are absent; two bare `SupabaseClient` instances signed
    in as the two accounts. No new secret is needed — `.secrets/live_sync.json` already carries all six
    keys. Never call `Supabase.initialize()`.

    Add a shared helper that signs both clients in, and an `addTearDown` that ends any buddy session it
    created (call `buddy_append_event` with kind `session_ended`, or update
    `buddy_sessions.ended_at`  — whichever the policies actually permit) so repeated runs do not
    accumulate live sessions.

    Write these five tests, named exactly as given so `-N` selection works:

    - `create and join round trip` — account A calls `buddy_create_session` with a random
      `workout_session_uuid` and a display name; asserts a non-null `buddy_session_id` and a non-empty
      `join_token`. Account B calls `buddy_join_session` with that token and its own
      `workout_session_uuid`; asserts the returned uuid equals A's `buddy_session_id`. Then asserts
      both accounts can `select` `buddy_participants` for that session and see two rows.
    - `token is single use` — B redeems the token successfully once, then a second
      `buddy_join_session` with the same token throws a `PostgrestException`.
    - `token gates` — three separate assertions, each throwing with the **same** message: a random
      unknown uuid as a token; a token whose session has been ended; and the host redeeming its own
      token. Assert the three exception messages are string-equal to each other. This is the
      existence-oracle check, and asserting equality is the point — do not merely assert each throws.
    - `join tokens are invisible` — both accounts `select` from `buddy_join_tokens`; assert the result
      is empty or the call throws. Either outcome satisfies deny-all; assert explicitly which one the
      server produced and record it in the summary.
    - `a departed participant cannot write` — B sets its own `left_at` via an update on
      `buddy_participants` (the one client-writable field), then calls `buddy_append_event` and is
      expected to throw. A's subsequent `buddy_append_event` on the same session must still succeed —
      this is BUD-06's server half, and asserting only the failure would miss the "the other's workout
      continues" requirement.

    Do not write the broadcast-delivery, gapless-seq or cross-user-table-negative tests here; those
    belong to plan 11-12 and need the client channel service. Leave a comment in the file naming them
    as the file's intended growth.
  </action>
  <verify>
    <automated>flutter test test/sync/live_buddy_test.dart --dart-define-from-file=.secrets/live_sync.json</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/sync/live_buddy_test.dart --dart-define-from-file=.secrets/live_sync.json` passes all five tests
    - `flutter test test/sync/live_buddy_test.dart` (no dart-defines) skips the group rather than failing
    - `flutter test test/sync/live_buddy_test.dart -N "token gates" --dart-define-from-file=.secrets/live_sync.json` selects and passes one test
    - The `token gates` test asserts the three failure messages are equal to one another, not merely that each throws
    - The file contains no hardcoded email, password, URL or anon key
    - The plan summary records whether `buddy_join_tokens` returned empty or threw
  </acceptance_criteria>
  <done>The server surface is proven live: tokens are single-use and opaque, and a departed participant cannot write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → live Supabase project | Real credentials and real rows; a bad migration is applied to production data |
| account B → account A's session | The cross-user boundary this whole phase is built around |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02 | Spoofing, Information disclosure | token enumeration / existence oracle | high | mitigate | The `token gates` test asserts the three distinct failure causes produce string-equal messages, so a later "friendlier error" refactor turns the suite red |
| T-11-01 | Spoofing | token reuse after redemption | high | mitigate | The `token is single use` test redeems twice and requires the second to throw |
| T-11-04 | Tampering | a departed participant still mutating | high | mitigate | `a departed participant cannot write` sets `left_at` then asserts both that B is refused and that A still succeeds |
| T-11-02 | Information disclosure | `buddy_join_tokens` readable by a client | high | mitigate | `join tokens are invisible` selects the table as both accounts and requires empty-or-throw |
| T-11-12 | Elevation of privilege | pushing to the SummitSki project | high | mitigate | Task 1 requires `supabase/config.toml`'s `project_id` to read `ldzgyzigvbwofbswitrv` before any command runs |
| T-11-18 | Tampering | applying a migration whose `realtime.send` assumption is wrong, leaving broadcast silently dead | high | mitigate | The migration's `do $$` preflight guard converts assumption A1 into a named `db push` failure; Task 1 requires the resolved signature to be recorded |
</threat_model>

<verification>
- `supabase migration list` shows 0011 remote
- `flutter test test/sync/live_buddy_test.dart --dart-define-from-file=.secrets/live_sync.json` green
- Run the live suite twice in a row and confirm both runs are green, matching the bar `docs/rb02-sync-verification.md` set for RB-02
</verification>

<success_criteria>
The buddy server surface exists in the Herculex project and its token lifecycle and participation
gate have been observed working against real accounts, so every later plan verifies against reality
rather than against a type-checker.
</success_criteria>

<output>
Create `.planning/phases/11-gym-buddy-live-workout/11-05-SUMMARY.md` when done.
</output>
