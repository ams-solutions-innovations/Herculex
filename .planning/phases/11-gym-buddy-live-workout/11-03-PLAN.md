---
phase: 11-gym-buddy-live-workout
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/data/local/tables.dart
  - lib/data/local/database.dart
  - lib/data/local/database.g.dart
  - drift_schemas/drift_schema_v29.json
  - test/generated_migrations/schema_v29.dart
  - test/generated_migrations/schema.dart
  - test/migration_test.dart
  - test/schema_v29_test.dart
  - test/buddy/buddy_local_only_test.dart
  - test/sync/sync_payload_test.dart
autonomous: true
requirements: [BUD-02, BUD-04]

must_haves:
  truths:
    - "A device upgrading from schema v28 lands on v29 with the two buddy mirror tables present and every pre-existing row intact"
    - "The two buddy mirror tables have no outbox trigger, so a buddy row can never be pushed to Supabase by SyncService"
    - "WorkoutSessions.buddySessionId survives a push/pull round trip, which is the only thing BUD-07 needs from this phase"
    - "A slot id resolves to a local workout_exercises row, which is what makes remove/reorder/replace addressable across two devices"
  artifacts:
    - path: "lib/data/local/tables.dart"
      provides: "BuddySessionsLocal and BuddyChoreographySlots table declarations plus WorkoutSessions.buddySessionId"
      contains: "class BuddyChoreographySlots"
    - path: "lib/data/local/database.dart"
      provides: "schemaVersion 29, the two tables registered in @DriftDatabase, and the from < 29 migration block"
      contains: "schemaVersion => 29"
    - path: "drift_schemas/drift_schema_v29.json"
      provides: "Generated v29 schema snapshot"
    - path: "test/generated_migrations/schema_v29.dart"
      provides: "Generated v29 migration fixture"
    - path: "test/schema_v29_test.dart"
      provides: "v28 -> v29 data-integrity migration test"
      min_lines: 40
    - path: "test/buddy/buddy_local_only_test.dart"
      provides: "Positive proof the two buddy mirror tables carry no outbox trigger and are in neither sync list"
      min_lines: 50
  key_links:
    - from: "lib/data/local/tables.dart"
      to: "WorkoutExercises"
      via: "BuddyChoreographySlots.workoutExerciseId references"
      pattern: "references\\(\\s*WorkoutExercises"
    - from: "test/buddy/buddy_local_only_test.dart"
      to: "sqlite_master"
      via: "trigger-name query on a fully migrated database"
      pattern: "sqlite_master"
    - from: "test/sync/sync_payload_test.dart"
      to: "workout_sessions.buddy_session_id"
      via: "push payload assertion"
      pattern: "buddy_session_id"
---

<objective>
Take the local schema from v28 to v29: two local-only buddy mirror tables and one nullable synced
column on `WorkoutSessions`, with every generated artifact regenerated and every migration-test chore
done rather than deferred.

Purpose: BUD-02 needs `buddySessionId` to travel with the session row so BUD-07 can compute a VS
comparison later; BUD-04 needs a durable `lastSeenSeq` and a `slotId -> local workout_exercises.id`
map so replay is ordered and addressable. Research Pitfall 9 records that forgetting the two
`drift_dev schema` commands produces a failure that reads like a migration bug but is a stale dump.

Output: schema v29, regenerated dumps and fixtures, an extended `migration_test.dart`, a new
`schema_v29_test.dart`, and the local-only proof.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gym-buddy-live-workout/11-RESEARCH.md
@lib/data/local/tables.dart
@lib/data/local/database.dart
@test/migration_test.dart
@test/schema_v28_test.dart
@test/rep_local_only_test.dart
</context>

<interfaces>
<!-- Table declarations this plan CREATES. Plans 11-07 and 11-11 read and write these. -->

class BuddySessionsLocal extends Table {          // local-only: NO SyncColumns, NO SyncTombstone
  TextColumn get buddySessionId => text()();      // uuid, server-assigned
  IntColumn  get workoutSessionId => integer()
      .references(WorkoutSessions, #id, onDelete: KeyAction.cascade)();
  TextColumn get role => text()();                // 'host' | 'guest'
  TextColumn get partnerDisplayName => text().nullable()();
  TextColumn get partnerAvatarUrl => text().nullable()();
  IntColumn  get lastSeenSeq => integer().withDefault(const Constant(0))();
  DateTimeColumn get joinedAt => dateTime()();
  DateTimeColumn get endedAt => dateTime().nullable()();
  @override Set<Column> get primaryKey => {buddySessionId};
}

class BuddyChoreographySlots extends Table {      // local-only
  TextColumn get buddySessionId => text()();
  TextColumn get slotId => text()();              // uuid, minted by whoever added the slot
  // Nullable: a slot whose exerciseRef this device cannot resolve is a visible
  // PLACEHOLDER with no local WorkoutExercises row (research Pitfall 8). Making
  // this non-null would make the placeholder unrepresentable and force the
  // silent-drop failure mode the pitfall exists to prevent.
  IntColumn  get workoutExerciseId => integer().nullable()
      .references(WorkoutExercises, #id, onDelete: KeyAction.cascade)();
  // The unresolved reference is retained so the slot can be resolved later
  // (partner rebuilds, catalogue updates) and so replace can target it.
  TextColumn get unresolvedUuid => text().nullable()();
  TextColumn get unresolvedSlug => text().nullable()();
  TextColumn get placeholderLabel => text().nullable()();
  // The SHARED choreography position. Distinct from WorkoutExercises.orderIndex,
  // which only orders rows that exist locally — a placeholder has none, but must
  // still hold its place in the shared ordering.
  IntColumn  get orderIndex => integer()();
  @override Set<Column> get primaryKey => {buddySessionId, slotId};
}
// Invariant, asserted in 11-07's tests: workoutExerciseId != null XOR
// (unresolvedUuid != null OR unresolvedSlug != null). A slot is either
// materialised locally or a placeholder — never both, never neither.

// WorkoutSessions gains exactly one column — the only change to a synced table in this phase:
TextColumn get buddySessionId => text().nullable()();
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declare the tables and the v29 migration block</name>
  <read_first>
    - lib/data/local/tables.dart lines 143-235 (`WorkoutSessions`, `WorkoutExercises`, `SetEntries`) and the `RepTrackingSettings`/`RepSetObservations` declarations further down — the local-only precedent that carries neither `SyncColumns` nor `SyncTombstone`
    - lib/data/local/database.dart lines 20-82 (the `@DriftDatabase(tables: [...])` list and `schemaVersion => 28`) — NOTE this file is currently modified in the working tree, so read the real file, not a memory of it
    - lib/data/local/database.dart lines 644-712 (the v26 local-only `createTable` block and the full v28 `from < 28 && to >= 28` block, including the `tryAddColumn` closure and the long comment explaining why the `to >=` half of the guard is load-bearing)
    - lib/data/local/migrations/sync_backfill.dart (`syncedTableNames`) and lib/data/sync/sync_table_specs.dart (`syncTableSpecs`) — both must remain byte-unchanged
  </read_first>
  <action>
    In `lib/data/local/tables.dart`, add `BuddySessionsLocal` and `BuddyChoreographySlots` exactly as
    given in `<interfaces>`. Neither mixes in `SyncColumns` or `SyncTombstone` — the authoritative rows
    live in Postgres and arrive over the buddy channel, and pushing them through the outbox would
    create a second conflicting delivery path for the same rows. Add a doc comment on each saying so.

    Add `TextColumn get buddySessionId => text().nullable()();` to `WorkoutSessions` with a doc comment
    stating it is nullable, null for every solo workout, and syncs so BUD-07 can compute a VS
    comparison later.

    In `lib/data/local/database.dart`:
    - Register `BuddySessionsLocal` and `BuddyChoreographySlots` in the `@DriftDatabase(tables: [...])`
      list, grouped with a comment mirroring the v26 rep-tracking grouping: "Gym Buddy (v29).
      Local-only: never added to syncedTableNames or syncTableSpecs."
    - Bump `schemaVersion` from 28 to 29.
    - Add to `onUpgrade` two blocks, following the v26 and v28 idioms respectively:
      * `if (from < 29) { await m.createTable(buddySessionsLocal); await m.createTable(buddyChoreographySlots); }`
        with no `sync_uuid` index and no `installSyncTriggers()` call.
      * `if (from < 29 && to >= 29) { ... await tryAddColumn(workoutSessions, workoutSessions.buddySessionId); }`
        — this block needs its own local `tryAddColumn` closure (the v28 one is scoped inside its own
        block) and needs the `to >=` half of the guard for the reason the v28 comment spells out:
        `SchemaVerifier.migrateAndValidate` fakes intermediate target versions, and `addColumn` has no
        `IF NOT EXISTS`.

    Do NOT touch `sync_backfill.dart` or `sync_table_specs.dart`. Per research assumption A8 and the
    v28 migration comment at `database.dart:667-676`, `SyncService._buildRemotePayload` derives the
    remote payload generically from whatever local columns exist beyond the registered
    FK/local-only/dateTime ones, so a plain nullable text column needs no spec entry.

    Then run `dart run build_runner build --delete-conflicting-outputs` to regenerate
    `lib/data/local/database.g.dart`.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze lib/data/local/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "schemaVersion => 29" lib/data/local/database.dart` returns 1
    - `grep -c "class BuddySessionsLocal\|class BuddyChoreographySlots" lib/data/local/tables.dart` returns 2
    - `BuddyChoreographySlots.workoutExerciseId` is declared `.nullable()` so a Pitfall 8 placeholder slot is representable, and the table carries `unresolvedUuid`, `unresolvedSlug`, `placeholderLabel` and a non-null `orderIndex`
    - Inserting a slot with `workoutExerciseId: null` and `unresolvedSlug: 'some-slug'` succeeds against a migrated database; the XOR invariant itself is asserted by 11-07's applier tests, not by a DB constraint
    - `grep -c "SyncColumns\|SyncTombstone" ` over the two new class bodies returns 0
    - `git diff --stat lib/data/local/migrations/sync_backfill.dart lib/data/sync/sync_table_specs.dart` is empty
    - `lib/data/local/database.g.dart` contains `BuddyChoreographySlots`
    - `flutter analyze lib/data/local/` reports 0 errors
  </acceptance_criteria>
  <done>Schema v29 is declared and code-generated.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the schema artifacts and extend the migration tests</name>
  <read_first>
    - test/migration_test.dart in full (the header comment naming the two `drift_dev schema` commands, and all seven `28` version literals across the current-schema test and the five `startAt(n)` replays)
    - test/schema_v28_test.dart in full (the `testWithDataIntegrity` shape, the `createOld`/`createNew` constructor pair, and the untouched-`gyms` canary proving the migration is purely additive)
    - test/generated_migrations/schema.dart (the `GeneratedHelper` the new v29 entry must appear in — it is regenerated, not hand-edited)
  </read_first>
  <action>
    Run, in order, from the repo root:
    1. `dart run drift_dev schema dump lib/data/local/database.dart drift_schemas/`
    2. `dart run drift_dev schema generate drift_schemas/ test/generated_migrations/`

    Confirm both `drift_schemas/drift_schema_v29.json` and `test/generated_migrations/schema_v29.dart`
    now exist and that `test/generated_migrations/schema.dart` registers v29. These are generated
    artifacts — never hand-write them; if either is missing, the dump command did not see
    `schemaVersion => 29` and Task 1 is incomplete.

    Edit `test/migration_test.dart`:
    - Update the header comment's `drift_schema_v28.json` reference to v29.
    - Change every `migrateAndValidate(db, 28)` to `migrateAndValidate(db, 29)` (six call sites: the
      current-schema test plus the `startAt(23)`, `startAt(24)`, `startAt(25)`, `startAt(26)` and
      `startAt(27)` replays), and rename the current-schema test's description to say v29.
    - Add a new `startAt(28)` replay test following the existing pattern, with a comment naming the
      v29 change: two local-only buddy mirror tables plus `workout_sessions.buddy_session_id`.

    Create `test/schema_v29_test.dart` following `test/schema_v28_test.dart` exactly:
    `testWithDataIntegrity(oldVersion: 28, newVersion: 29, createOld: v28.DatabaseAtV28.new,
    createNew: v29.DatabaseAtV29.new, openTestedDatabase: AppDatabase.forTesting, ...)`. In
    `createItems`, insert a `workout_sessions` row (rather than the `gyms` canary v28 used) so the
    validation can assert two things at once: the pre-existing session survived, and its new
    `buddy_session_id` reads as NULL after the migration. Also assert `gyms` is untouched, keeping
    v28's purely-additive canary.
  </action>
  <verify>
    <automated>flutter test test/migration_test.dart test/schema_v29_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test -f drift_schemas/drift_schema_v29.json && test -f test/generated_migrations/schema_v29.dart` succeeds
    - `grep -c "migrateAndValidate(db, 28)" test/migration_test.dart` returns 0
    - `grep -c "startAt(28)" test/migration_test.dart` returns 1
    - `flutter test test/migration_test.dart test/schema_v29_test.dart` passes with 7 migration tests plus the v29 data-integrity test
    - `test/schema_v29_test.dart` asserts the migrated `workout_sessions` row has a NULL `buddy_session_id`
  </acceptance_criteria>
  <done>The schema snapshot, fixtures and migration tests are all at v29.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the buddy mirror tables are local-only and buddy_session_id round-trips</name>
  <read_first>
    - test/rep_local_only_test.dart in full — this is the direct template: it queries `sqlite_master` on a fully migrated database rather than grepping `tables.dart`, because `installSyncTriggers` writes DDL from a name list and a source grep would say nothing about it; note the `expect(rows, isNotEmpty)` guard that stops the assertion passing vacuously
    - test/sync/sync_payload_test.dart lines 1-60 (the harness, the `syncUuid` helper and how a push payload is asserted)
    - lib/data/sync/sync_service.dart lines 455-520 (`_buildRemotePayload` — the generic derivation that is the reason `buddy_session_id` needs no spec entry)
  </read_first>
  <action>
    Create `test/buddy/buddy_local_only_test.dart`, a clone of `test/rep_local_only_test.dart` scoped
    to the two table names `buddy_sessions_local` and `buddy_choreography_slots`:
    - Open a fully migrated database via `openTestDatabase()`.
    - Query `SELECT name, tbl_name FROM sqlite_master WHERE type = 'trigger'`, assert the result set is
      non-empty (otherwise the check passes vacuously), and assert neither buddy table name appears as
      a `tbl_name`.
    - Assert neither name appears in `syncedTableNames` (from `lib/data/local/migrations/sync_backfill.dart`)
      nor as a key in `syncTableSpecs` (from `lib/data/sync/sync_table_specs.dart`).
    - Add a `PRAGMA table_info` check that `buddy_sessions_local` has no `sync_uuid`, `updated_at` or
      `deleted_at` column — the three columns `SyncColumns`/`SyncTombstone` would have added.

    Extend `test/sync/sync_payload_test.dart` with one test named exactly `buddy_session_id` (so
    `flutter test test/sync/sync_payload_test.dart -N "buddy_session_id"` selects it): start a session
    on device A, set its `buddySessionId` to a known uuid, push, and assert the remote payload
    `SyncService` produced contains key `buddy_session_id` with that value; then pull onto device B and
    assert B's `workout_sessions` row carries the same value. This is BUD-07's only dependency on this
    phase and the concrete check on research assumption A8.
  </action>
  <verify>
    <automated>flutter test test/buddy/buddy_local_only_test.dart && flutter test test/sync/sync_payload_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/buddy/buddy_local_only_test.dart` passes with at least 3 tests
    - The trigger test contains an `isNotEmpty` guard on the `sqlite_master` result set
    - `flutter test test/sync/sync_payload_test.dart -N "buddy_session_id"` selects and passes exactly one test
    - `flutter test` (full offline suite) shows no regression against the 538-passing baseline
  </acceptance_criteria>
  <done>BUD-02's local half is proven: buddy rows never enter the outbox, and `buddySessionId` does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Drift → Supabase outbox | Any table with an outbox trigger pushes its rows under the local user's `user_id` |
| partner's choreography → local database | Slot rows arriving from the channel land in a local-only mirror, never in a synced table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-15 | Information disclosure | `buddy_sessions_local` / `buddy_choreography_slots` gaining an outbox trigger | medium | mitigate | Declared without `SyncColumns`/`SyncTombstone`, added to neither `syncedTableNames` nor `syncTableSpecs`, and proven by a `sqlite_master` trigger query on a fully migrated database rather than by a source grep |
| T-11-16 | Tampering | v29 migration silently dropping rows on upgrade | medium | mitigate | `test/schema_v29_test.dart` uses `testWithDataIntegrity` with a pre-existing `workout_sessions` row and an untouched-`gyms` canary; `migration_test.dart` replays every generated fixture from v23 through v28 |
| T-11-09 | Information disclosure | A partner's custom exercise materialised into the local synced `exercise_catalog` | medium | mitigate | Out of scope for this plan by construction — the slot table stores a local `workoutExerciseId`, never a catalogue row; the sender-side gate lands in plan 11-08 and the receiver-side placeholder in 11-07 |
</threat_model>

<verification>
- `flutter test test/migration_test.dart test/schema_v29_test.dart test/buddy/ test/sync/sync_payload_test.dart` is green
- `flutter test` full offline suite green with no regression
- `git diff --stat lib/data/local/migrations/sync_backfill.dart lib/data/sync/sync_table_specs.dart` is empty
</verification>

<success_criteria>
The app opens at schema v29 on a fresh install and on an upgrade from any generated fixture back to
v23, the two buddy mirror tables are provably outside the sync path, and `buddySessionId` round-trips
through push/pull.
</success_criteria>

<output>
Create `.planning/phases/11-gym-buddy-live-workout/11-03-SUMMARY.md` when done.
</output>
